import 'package:flutter/material.dart';

class CafeSmallTile extends StatelessWidget {
  const CafeSmallTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
